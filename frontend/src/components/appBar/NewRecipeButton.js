import Button from '@material-ui/core/Button';

const NewRecipeButton = () => {
    return (
        <div className="NewRecipeButtonDiv">
            <Button className="NewRecipeButton" color="inherit">Add new recipe</Button>
        </div>
    );
}

export default NewRecipeButton;